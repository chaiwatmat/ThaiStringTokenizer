name: build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2.108
      - name: Build with dotnet
        run: |
          dotnet build --configuration Debug
      - name: Run unit test result opencover
        run: |
          dotnet test \
             --no-build \
            /p:CollectCoverage=true \
            /p:CoverletOutput=coverage.xml \
            /p:CoverletOutputFormat=opencover \
            /p:Threshold=80 \
            /p:ThresholdType=branch
      - name: Run unit test result lcov
        run: |
          dotnet test \
             --no-build \
            /p:CollectCoverage=true \
            /p:CoverletOutputFormat=lcov \
            /p:Threshold=80 \
            /p:ThresholdType=branch
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ThaiStringTokenizerTest/coverage.xml
      - name: Coveralls Parallel
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path-to-lcov: ./ThaiStringTokenizerTest/coverage.info # optional (default value)
      - name: Coveralls Finished
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          parallel-finished: "true"
      # - name: Push to nuget
      #   env:
      #     NUGET_TOKEN: ${{ secrets.NUGET_TOKEN }}
      #   run: dotnet nuget push ThaiStringTokenizer/bin/Debug/*.nupkg -k $NUGET_TOKEN -s https://api.nuget.org/v3/index.json
